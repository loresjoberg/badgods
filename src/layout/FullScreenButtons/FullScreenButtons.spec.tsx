import React from 'react';
import { expect } from 'chai';
import FullScreenButtons from './FullScreenButtons';

describe('Spec FullScreenButtons', function () {
    it('it exists', () => {
        expect(FullScreenButtons).to.be.ok;
    });
});
