import React from 'react';
import { expect } from 'chai';
import Navarrow from './NavArrow';

describe('Spec Navarrow', function () {
    it('it exists', () => {
        expect(Navarrow).to.be.ok;
    });
});
