import React from 'react';
import { expect } from 'chai';
import Backbutton from './BackButton';

describe('Spec Backbutton', function () {
    it('it exists', () => {
        expect(Backbutton).to.be.ok;
    });
});
