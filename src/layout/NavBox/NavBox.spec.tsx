import React from 'react';
import { expect } from 'chai';
import NavBox from './NavBox';

describe('Spec NavBox', function () {
    it('it exists', () => {
        expect(NavBox).to.be.ok;
    });
});
