import React from 'react';
import { expect } from 'chai';
import Content from './Content';

describe('Spec Content', function () {
    it('it exists', () => {
        expect(Content).to.be.ok;
    });
});
