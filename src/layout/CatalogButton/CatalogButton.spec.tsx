import React from 'react';
import { expect } from 'chai';
import CatalogButton from './CatalogButton';

describe('Spec CatalogButton', function () {
    it('it exists', () => {
        expect(CatalogButton).to.be.ok;
    });
});
