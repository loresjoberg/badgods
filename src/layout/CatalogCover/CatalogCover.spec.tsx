import React from 'react';
import { expect } from 'chai';
import CatalogCover from './CatalogCover';

describe('Spec CatalogCover', function () {
    it('it exists', () => {
        expect(CatalogCover).to.be.ok;
    });
});
