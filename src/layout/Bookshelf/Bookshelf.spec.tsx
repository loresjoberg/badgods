import React from 'react';
import { expect } from 'chai';
import Bookshelf from './Bookshelf';

describe('Spec Bookshelf', function () {
    it('it exists', () => {
        expect(Bookshelf).to.be.ok;
    });
});
