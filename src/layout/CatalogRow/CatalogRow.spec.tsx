import React from 'react';
import { expect } from 'chai';
import CatalogRow from './CatalogRow';

describe('Spec CatalogRow', function () {
    it('it exists', () => {
        expect(CatalogRow).to.be.ok;
    });
});
