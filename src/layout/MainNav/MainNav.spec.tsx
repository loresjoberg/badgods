import React from 'react';
import { expect } from 'chai';
import MainNav from './MainNav';

describe('Spec MainNav', function () {
    it('it exists', () => {
        expect(MainNav).to.be.ok;
    });
});
