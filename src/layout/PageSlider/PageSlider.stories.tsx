import React from 'react';
import PageSlider, { PageSliderProps } from './PageSlider';

export default {
    title: "PageSlider",
    component: PageSlider
};

export const Default = (props: PageSliderProps) => <PageSlider {...props} />;
