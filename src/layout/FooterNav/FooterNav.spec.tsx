import React from 'react';
import { expect } from 'chai';
import Footernav from './FooterNav';

describe('Spec Footernav', function () {
    it('it exists', () => {
        expect(Footernav).to.be.ok;
    });
});
